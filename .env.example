## Override the backend URL the PWA talks to.
## Leave unset to use the prod default (https://figsapp.duckdns.org/api).
##
## Useful for local backend dev:
##   cp .env.example .env.local
##   echo 'VITE_API_BASE=http://localhost:8000/api' > .env.local
# VITE_API_BASE=http://localhost:8000/api
